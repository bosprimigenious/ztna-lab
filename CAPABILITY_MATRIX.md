# 能力对照矩阵

本表以 VPN/ZTNA 客户端的可观察能力为参照，不声称掌握或复制北邮部署及
aTrust 的内部协议。目标是让自有系统取得相近的使用结果，同时保持协议、密钥、
域名和基础设施完全独立。

状态定义：`已实现（本机）` 表示有自动化测试；`合约模拟` 表示接口已成形但没有
操作真实网络；`待生产化` 表示必须接入自有基础设施后才能验证；`明确不做` 表示
不属于本项目。

| 用户侧能力 | 当前独立实现 | 状态 | 自有网关生产要求 |
| --- | --- | --- | --- |
| 账号登录 | 随机实验账号 + PBKDF2 密码校验 | 已实现（本机） | OIDC/SAML/LDAP IdP、风控、锁定策略 |
| 二次认证 | RFC 6238 形态 TOTP，计数器单次使用 | 已实现（本机） | IdP MFA/WebAuthn/硬件密钥 |
| 设备注册 | 登录提交 Ed25519 公钥，数据/控制请求均证明持有 | 已实现（本机） | 首次登记审批、轮换、丢失设备吊销 |
| 设备姿态 | OS、managed、版本规则 | 合约模拟 | MDM/EDR/TPM/Secure Boot 可信取证 |
| 短期访问授权 | Ed25519 grant，资源/scope/会话/设备/租约绑定 | 已实现（本机） | KMS/HSM、多 key ID、JWKS/轮换 |
| 资源清单 | 签名的资源、路由、DNS、会话清单 | 已实现（本机） | 按用户过滤、版本发布和回滚 |
| 数据网关 | TLS 1.3、verify-only key、策略二次检查 | 已实现（本机） | 独立进程、公网入口、mTLS 管理面、HA |
| TCP 应用访问 | 帧化单流、固定 allowlist HTTP GET | 已实现（本机） | 标准 CONNECT/QUIC 代理或资源连接器、流控 |
| L3 隧道 | 合成 IPv4 帧、源 VIP 与目标 CIDR 检查 | 合约模拟 | WireGuard 或 IKEv2/IPsec，不用自研加密 |
| 虚拟网卡 | 仅分配 `100.64.0.0/24` 内存 VIP | 合约模拟 | TUN/VNIC 生命周期、MTU、IPv6、冲突检测 |
| 分流路由 | 签名清单、最长前缀匹配、默认拒绝 | 合约模拟 | 特权助手原子安装/回滚、LAN 排除项 |
| 域名映射 | `198.18.0.0/24` 内存 Fake DNS | 合约模拟 | 受控 DNS stub、TTL、搜索域、DNSSEC 策略 |
| 心跳/续租 | 设备签名心跳刷新会话、grant 和清单；保留绝对期限 | 已实现（本机） | 客户端/网关/控制面三类心跳和故障判定 |
| 登出/撤销 | 会话撤销，后续网关连接拒绝，IP 回收 | 已实现（本机） | 吊销推送、活动流关闭、IP 隔离期 |
| 断线重连 | 客户端可重新登录 | 合约模拟 | 指数退避、会话恢复、网关切换 |
| 审计 | 无密钥/密码/载荷的有界内存事件 | 已实现（本机） | 持久管道、事件 ID、RBAC、留存、防篡改 |
| 客户端网络应用 | 验签、diff、内存 apply/rollback | 合约模拟 | 最小权限 helper，事务式 TUN/路由/DNS |
| aTrust 协议/网关兼容 | 无 | 明确不做 | 只有厂商公开 SDK/授权集成才可另立项目 |
| 北邮校园网接入 | 无 | 明确不做 | 不使用北邮入口、账号、证书或网络配置 |

## 验收口径

达到“自有测试网可用”至少还要证明：控制面和网关独立部署；真实 PKI 与密钥轮换；
WireGuard/IPsec 隧道；客户端路由/DNS 原子回滚；可信姿态；集中撤销；持久审计；
网关故障切换；性能/滥用/模糊测试；以及在书面授权的隔离网段完成端到端测试。

当前 33 项测试只支持“回环样机通过”，不支持“生产 VPN 已完成”的结论。

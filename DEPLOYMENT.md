# 自有网关部署路线

## 目标拓扑

```text
                              +--> IdP / MFA
Client --> Control API :443 --+--> Policy + Device posture
   |                          +--> Session/IPAM + KMS signer
   |
   +==== WireGuard/IPsec ====> Data Gateway ---> Resource Connector ---> 私有资源
                                  ^
                                  |
                     mTLS 管理通道：公钥、策略版本、吊销、健康状态
```

公网控制入口、数据入口和私有管理入口必须是不同身份。资源连接器只接受管理面下发
的 allowlist，客户端永远不能指定任意目标地址。

## 分阶段实施

### P0：回环契约样机（当前）

- 验证认证、授权、设备持有证明、清单、路由模型、会话、审计和帧状态机。
- 所有地址锁定 `127.0.0.1`，网络助手不修改操作系统。
- 出口：33 项自动化测试和可重复 demo。

### P1：拆分控制面与网关

- 将会话/IPAM、资源注册、策略和审计迁移到持久服务。
- 控制面与网关使用独立服务证书；管理通道强制双向 TLS。
- grant 私钥进入 KMS/HSM；网关只同步带版本的验签公钥。
- 引入策略 epoch、租约代际和吊销事件；重启后防重放状态不能倒退。
- 出口：两个独立进程/主机测试、密钥轮换、撤销延迟和重启恢复证据。

### P2：标准 L3 数据面

- 默认选 WireGuard；已有企业 VPN/合规生态时可选 IKEv2/IPsec。
- 控制面只负责认证、策略和配置分发，数据加密由标准实现承担。
- 网关验证源虚拟 IP、租约代际、允许 CIDR、协议和端口；默认无全隧道。
- 增加 MTU/分片、IPv6、速率/字节/连接数限制和 DDoS 防护。
- 出口：隔离测试网双向连通、越权 CIDR 失败、吞吐/时延和密钥轮换测试。

### P3：客户端网络助手

- 非特权客户端验证控制面签名；最小权限 helper 再独立验签。
- helper 预检路由冲突、LAN/保留网段、DNS 冲突和版本，然后事务式应用。
- 崩溃、登出、吊销、清单过期或升级失败必须恢复原路由/DNS/TUN 状态。
- Windows 可评估 Wintun/WireGuard 官方组件；不要从零实现内核驱动或密码学。
- 出口：安装/升级/卸载、崩溃恢复、无残留路由/DNS、低权限边界测试。

### P4：生产可靠性

- 多可用区网关、健康探测、容量调度、客户端退避和故障切换。
- 可信 MDM/EDR/TPM 姿态，设备证书轮换与丢失设备吊销。
- 追加式审计、管理员 RBAC、告警、留存、隐私和密钥访问审计。
- 安全评审、第三方渗透/协议模糊测试、灾备演练和发布回滚。

## PKI 与密钥边界

| 用途 | 密钥持有者 | 建议 |
| --- | --- | --- |
| 控制 API TLS | 控制服务 | ACME/企业 CA 短周期证书 |
| 数据网关 TLS/mTLS | 每个网关实例 | 独立证书、SAN 限定、自动轮换 |
| grant 签名 | KMS/HSM | Ed25519/ECDSA，发布 verify-only key set |
| 清单签名 | 独立 KMS/HSM key | 与 grant 分权，支持 revision/expiry |
| 设备身份 | TPM/OS keystore | 不可导出私钥、登记和吊销 |
| L3 隧道 | WireGuard/IPsec 端点 | 使用标准握手、轮换和重放保护 |

不把密码、TOTP seed、签名私钥或 WireGuard 私钥放进配置仓库。日志只记录主体、
设备指纹、策略/网关版本、决策、字节数和时长，不记录 token、OTP 或原始载荷。

## 接真实自有网关前的输入

需要由网络所有者提供并书面确认：

- 网关操作系统/区域、控制域名和数据域名，以及公网/私网监听端口
- 可访问资源清单：CIDR、域名、协议、端口、资源 owner 和审批人
- IdP/MFA、MDM/EDR、PKI/KMS 选型与管理员责任边界
- 虚拟地址池、现有 LAN/VPC 路由、DNS zone、IPv6 和 MTU 约束
- 预计并发/带宽、HA/RTO/RPO、日志留存与合规要求
- 隔离测试环境和明确的授权测试窗口

在这些输入确定前，项目保持回环模式；不要把监听地址改成 `0.0.0.0`，也不要
直接加入系统路由或尝试连接任何第三方 VPN 网关。

# ZTNA Lab 设计

## 范围与目标

本项目只为自有网络验证 ZTNA/VPN 的职责划分。它不读取或使用 aTrust 的
二进制、凭据、票据、证书、路由和校园网配置，也不尝试兼容其专有协议。
“功能对应”指用户侧结果相近，不代表线协议、内部实现或安全等级相同。

当前所有服务和出站资源都被代码限制为精确 IPv4 字面量 `127.0.0.1`；端口
临时分配，网关不接受客户端提供的目标主机或端口。

## 信任边界

| 组件 | 当前职责 | 生产部署职责 |
| --- | --- | --- |
| 客户端 | 登录、设备密钥、验证清单、发起 TCP/L3 帧 | 非特权 UI/会话进程 |
| 控制面 | MFA、姿态、策略、会话、IP 租约 | 接 IdP/MDM、策略数据库和租约服务 |
| 授权签发器 | Ed25519 私钥签发短期 grant | 私钥进入 KMS/HSM，控制面只请求签名 |
| 清单签发器 | Ed25519 签名资源/路由/DNS | 独立版本、轮换、撤回和灰度发布 |
| 数据网关 | 仅持公钥，验证会话/设备/策略后接资源 | 独立进程/主机，多可用区和限流 |
| 资源注册表 | 固定 allowlist | 管理员审批、协议/端口级约束 |
| 网络助手 | 验签、diff、内存应用/回滚 | 唯一可操作 TUN/路由/DNS 的最小权限进程 |
| 审计 | 有界内存事件 | 追加式持久存储、RBAC、留存和防篡改 |

当前控制面与网关共享内存会话/策略，只是可执行的契约样机；生产实现必须
通过认证管理通道和持久存储解耦，不能共享 Python 对象。

## 控制面流程

1. 客户端生成临时 Ed25519 设备密钥，提交用户名、密码、TOTP、公钥和姿态声明。
2. 控制面验证 MFA 与策略，建立带绝对/空闲期限的会话，分配虚拟 IP 和租约代际。
3. 授权签发器产生 120 秒 grant，绑定 `sub`、`aud`、`sid`、资源、scope、设备
   指纹、虚拟 IP、租约代际、清单哈希/版本、`iat`、`exp` 和 `jti`。
4. 后续清单、心跳和登出请求也必须由设备私钥签名；仅持有 Bearer token 不能续租。
5. 清单签发器返回独立 Ed25519 签名信封，包含资源、CIDR、Fake DNS、虚拟 IP
   和租约信息。客户端先验签、再检查哈希和安全约束。
6. 心跳在绝对期限内刷新会话、120 秒 grant 和清单；登出、空闲或过期会撤销
   会话并回收 IP。

姿态目前只是声明值加设备密钥持有证明，不等同 TPM、Secure Boot、EDR 或 MDM
的可信证明。生产策略必须从可信设备服务取证。

## 数据面流程

1. 客户端用 TLS 1.3 连接数据网关，发送短期 grant。
2. 客户端针对 token 哈希、资源、模式、请求 ID 和时间戳签名；网关用会话中
   登记的设备公钥验签，并拒绝过期或重复请求 ID。
3. 网关只持授权验签公钥，并重新检查会话、租约、设备、清单版本和策略。
4. TCP 模式使用 `TCP_OPEN -> TCP_DATA -> FIN` 的版本化帧，只支持单个固定
   HTTP GET 资源；L3 模式只验证一个合成 IPv4 数据包并返回确认，不发 raw packet。
5. 帧头包含 magic、版本、类型、flags、stream ID、每流序号和长度。解码器拒绝
   超长、截断、重放、序号跳跃、流类型混用和非法状态转换。

样机不是通用代理：没有 SOCKS、CONNECT、客户端选址、UDP 转发或端口扫描面。

## 加密设计

- 传输机密性与完整性完全交给 TLS 1.3 和操作系统/OpenSSL，不自定义 cipher。
- grant 与路由清单使用相互独立的 Ed25519 密钥；网关只有 grant 公钥。
- 设备每次连接以 Ed25519 签名证明持有登录时登记的私钥。
- 密码样机使用 PBKDF2-HMAC-SHA256；TOTP 遵循 RFC 6238 形态并阻止同一计数器复用。
- 当前 CA、控制面/网关独立叶证书和签名私钥均为每次运行临时生成。正式环境
  必须使用独立服务身份、短周期证书、KMS/HSM、轮换、吊销和 mTLS 管理面。
- 真实 L3 不复用本项目的合成包格式。生产优先使用审计成熟的 WireGuard；若有
  企业互通/合规需求则使用 IKEv2/IPsec。应用控制仍可走 TLS 1.3。

## 失败关闭规则

- 未知清单/帧版本、签名不符、清单过期或哈希不符：拒绝。
- 非分流清单、默认路由、可合并成全 IPv4 的路由集合、非实验 IP 池、重复 DNS
  或同前缀多资源：拒绝。
- grant、会话、设备、公钥、虚拟 IP、租约代际或资源任一不一致：拒绝。
- 数据流超时、超限、状态非法、撤销或上游异常：关闭连接。
- 网络助手只能消费已验签清单；当前实现的 `mutates_os` 恒为 `False`。

## 当前非目标

尚未实现真实 TUN/VNIC、WFP/NetFilter、系统路由/DNS、网关公网监听、持久身份、
可信硬件姿态、集中吊销、HA、流量拥塞控制或生产审计。真实部署步骤见
[DEPLOYMENT.md](DEPLOYMENT.md)。
